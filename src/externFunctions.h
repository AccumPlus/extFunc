#ifndef EXTERNFUNCTIONS_H
#define EXTERNFUNCTIONS_H

#include <string>
#include <list>
#include <vector>
#include <ctime>

std::list<std::string> split (const std::string temp, const char separator);

#endif
